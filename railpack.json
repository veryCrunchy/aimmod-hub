{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22"
  },
  "steps": {
    "install-web": {
      "inputs": ["."],
      "commands": [
        "npm install -g corepack",
        "corepack enable",
        "pnpm install --no-frozen-lockfile"
      ]
    },
    "build-web": {
      "inputs": [{ "step": "install-web" }],
      "commands": ["pnpm build:web"],
      "deployOutputs": [{ "include": ["web/dist/**"] }]
    }
  },
  "deploy": {
    "variables": {
      "AIMMOD_HUB_STATIC_DIR": "/app/web/dist"
    }
  }
}
